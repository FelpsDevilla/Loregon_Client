import { AcervoItem } from '../../../shared/models/Archive/acervo-item';

describe('AcervoItem', () => {
  it('should create an instance', () => {
    expect(new AcervoItem()).toBeTruthy();
  });
});
